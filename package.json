{
  "name": "signalflow-studio",
  "version": "0.1.0",
  "private": true,
  "description": "One-app Next.js deployment for SignalFlow Studio.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start"
  },
  "engines": {
    "node": ">=18"
  }
}
