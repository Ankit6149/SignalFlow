"""SignalFlow core package - Context ingestion and tooling (safe subset).

This package provides a directory walker and SNR-based file scoring
for identifying high-signal code blocks to forward into an ML context.
"""

__version__ = "0.1.0"
